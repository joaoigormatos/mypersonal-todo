export type TodoDTO = {
  title: string;
  description: string;
  date: Date;
  userId: string;
};

export type Todo = {
  id: string;
  title: string;
  description: string;
  date: Date;
  userId: string;
};
